import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
  container: {
    marginTop: 20,
    alignItems: "center",
    marginBottom: 200,
    minHeight: 800,
  },
});
