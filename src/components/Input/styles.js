import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
  container: {
    height: 45,
    width: 360,
    borderWidth: 2,
    borderRadius: 5,
    backgroundColor: 'white',
    flexDirection: 'row',
    alignItems: 'center',
  },
  icon: {
    marginLeft: 'auto',
    marginRight: 'auto',
  },
  input: {
    width: 300,
    fontSize: 15,
  },
});
