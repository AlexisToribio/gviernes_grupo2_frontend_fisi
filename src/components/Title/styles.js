import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
  title: {
    fontFamily: 'Roboto',
    fontSize: 48,
    fontWeight: 'bold',
    textAlign: 'center',
  },
});
