import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  pickerContainer: {
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "center",
    borderColor: "black",
    borderRadius: 4,
    borderWidth: 1,
    height: 40,
    width: "100%",
  },

  icon: {
    marginTop: 10,
    width: 30,
    height: 30,
  },
});
